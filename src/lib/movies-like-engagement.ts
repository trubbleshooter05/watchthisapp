/**
 * Movie-like guides that show the “Top picks” strip + optional “Continue watching” rows.
 * Keep in sync with GSC / product priorities (horror cluster, flagship blockbusters).
 */
export const MOVIES_LIKE_ENGAGEMENT_SLUGS = new Set<string>([
  "top-gun-maverick",
  "the-unhealer",
  "hereditary",
  "get-out",
  "the-conjuring",
  "men",
]);
